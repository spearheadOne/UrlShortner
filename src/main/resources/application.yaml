ktor:
  deployment:
    port: 8080
  application:
    modules:
      - org.abondar.experimental.urlshortner.ApplicationKt.module

  redis:
    url: "redis://localhost:6379"
    db: 0
    password: ""

  cassandra:
    contactPoints: "localhost:9042"
    localDatacenter: "datacenter1"

  urlshortner:
    defaultTtlSeconds: 86400
    snowflake:
      retryCount: 3
      url: "http://localhost:8081/snowflake"